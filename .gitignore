# The dataset itself — challenge use only, no redistribution. Never commit.
export/
*.tar.gz
*.zip

# Anything regenerable that carries dataset content (per-task rows, prompts,
# recovered responses). Charts and aggregate summaries ARE committed, so the
# jury can see the evidence without the data.
**/results/*.jsonl
**/results/*.csv
**/results/payloads*
evaluation/results/

__pycache__/
.DS_Store

# Presentation speaking notes — working material, not a deliverable.
submission/SCRIPT.md
**/PITCH-SCRIPT.md
